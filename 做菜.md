# 做菜网页需求说明

## 1. 项目目标

开发一个做菜管理网页，帮助用户根据冰箱库存推荐可做菜品，并管理菜谱、食材、购物清单和当天计划制作的菜。

项目目标部署形态为：前端页面部署在 Vercel，后端 API 使用 Vercel Serverless Functions，数据库使用 Supabase PostgreSQL。浏览器 `localStorage` 仅作为本地预览和接口异常时的兜底缓存。页面需要同时适配 PC 和移动端。

## 2. 技术要求

- 前端技术栈：HTML + CSS + JavaScript，当前不强制引入 React/Vue。
- 部署平台：Vercel。
- 后端接口：Vercel Serverless API，接口目录为 `api/`。
- 数据库：Supabase PostgreSQL。
- 本地缓存：`localStorage` 仅用于静态预览、离线兜底和远程接口失败兜底。
- 本地连库调试必须使用 `vercel dev`，不能使用 IDE 的 `63342` 静态预览服务访问远程 API。

## 3. 信息架构

应用分为前台和后台两个区域。

### 3.1 前台页面

- 今日推荐
- 今日菜品
- 菜谱
- 冰箱
- 购物清单
- 做菜历史

### 3.2 后台页面

- 食材分类管理
- 食材管理
- 菜谱管理
- 数据备份与恢复

前台和后台的页面标签支持拖动排序，排序结果保存到本地，刷新后保持用户调整后的顺序。

## 4. 数据模型与数据库设计

库存数量统一存放在食材表的 `stock` 字段中，不再单独维护 `fridge` 数组，避免同一份库存数据重复保存。

生产数据库采用 Supabase PostgreSQL 拆表设计，建表脚本位于 `database/schema.sql`。

核心数据表：

- `app_categories`：食材分类。
- `app_ingredients`：食材、库存、单位、保质期。
- `app_tags`：菜谱标签。
- `app_recipes`：菜谱主体、图片信息、制作步骤 JSON。
- `app_recipe_tags`：菜谱和标签关联。
- `app_today_dishes`：今日菜品安排。
- `app_shopping_items`：购物清单。
- `app_cook_history`：做菜历史。
- `app_settings`：临期预警天数、页面排序等设置。

前端业务层仍使用统一 JSON 结构，便于导入导出和兼容旧接口：

```js
{
  categories: [
    { id: "cat_1", name: "肉类" }
  ],
  ingredients: [
    {
      id: "ing_1",
      name: "鸡蛋",
      categoryId: "cat_2",
      stock: 6,
      unit: "个",
      expireAt: "2026-05-18"
    }
  ],
  recipes: [
    {
      id: "rec_1",
      name: "番茄炒蛋",
      desc: "家常快手菜",
      image: {
        id: "img_1",
        name: "tomato-egg.jpg",
        mimeType: "image/jpeg",
        storageType: "local",
        url: "data:image/jpeg;base64,..."
      },
      coverColor: "#f97316",
      tags: ["快手菜", "家常"],
      difficulty: "简单",
      favorite: true,
      steps: [
        {
          id: "step_1",
          content: "鸡蛋打散，番茄切块。",
          time: 180,
          consumes: [
            { ingredientId: "ing_1", count: 2 }
          ]
        }
      ]
    }
  ],
  todayDishes: [
    {
      id: "today_1",
      recipeId: "rec_1",
      mealType: "lunch",
      status: "pending",
      createdAt: 1778198400000
    }
  ],
  shoppingList: [
    { id: "shop_1", ingredientId: "ing_2", name: "番茄", count: 2, unit: "个", checked: false }
  ],
  cookHistory: [
    { id: "his_1", recipeId: "rec_1", recipeName: "番茄炒蛋", cookedAt: 1778198400000 }
  ],
  meta: {
    version: 2,
    storageMode: "remote",
    expireWarningDays: 3,
    frontPageOrder: ["recommend", "today", "recipes", "fridge", "shopping", "history"],
    adminPageOrder: ["categories", "ingredients", "recipeAdmin", "backup"],
    exportedAt: null
  }
}
```

字段说明：

- `id` 使用前端生成的唯一字符串。
- `ingredients.stock` 表示当前冰箱库存。
- `ingredients.unit` 表示单位，例如 `个`、`克`、`颗`、`勺`。
- `ingredients.expireAt` 表示保质期日期，可为空。
- 第一版只展示单位，不做克、斤、个、勺等单位之间的自动换算。
- `recipes.tags` 表示菜谱标签，例如 `快手菜`、`下饭菜`、`早餐`、`低脂`。
- `recipes.difficulty` 可取 `简单`、`中等`、`复杂`。
- `recipes.favorite` 表示是否收藏。
- `recipes.image` 表示菜品图片。第一版使用本地 `dataURL` 保存，后续可替换为服务器文件地址或对象存储地址。
- `recipes.image.storageType` 可取 `local`、`remote`、`objectStorage`，为后续接入数据库和文件存储预留扩展。
- `steps.content` 表示当前步骤要做什么。
- `steps.time` 单位为秒，非必填；为空或 0 时表示该步骤不需要计时。
- `steps.consumes` 表示该步骤会消耗的食材及数量。
- `todayDishes.status` 可取 `pending`、`cooking`、`done`。
- `todayDishes.mealType` 可取 `breakfast`、`lunch`、`dinner`、`snack`。
- `shoppingList` 表示购物清单。
- `cookHistory` 表示制作完成历史。
- `meta.version` 表示数据结构版本，用于后续导入兼容。
- `meta.storageMode` 表示当前存储模式，第一版为 `local`，后续接入数据库时可扩展为 `remote`。
- `meta.expireWarningDays` 表示临期预警天数，默认 3 天，可在后台设置。
- `meta.frontPageOrder` 表示前台页面标签排序。
- `meta.adminPageOrder` 表示后台页面标签排序。

### 4.1 API 设计

后端 API 按模块拆分，避免所有业务都挤在一个接口中。保留 `/api/data` 聚合接口作为兼容层，现有前端可继续使用；新增页面或重构前端时应优先使用模块 API。

模块 API：

- `GET/POST /api/categories`
- `PUT/DELETE /api/categories/:id`
- `GET/POST /api/ingredients`
- `PUT/DELETE /api/ingredients/:id`
- `GET/POST /api/tags`
- `PUT/DELETE /api/tags/:id`
- `GET/POST /api/recipes`
- `PUT/DELETE /api/recipes/:id`
- `GET/POST /api/shopping`
- `PUT/DELETE /api/shopping/:id`
- `GET /api/snapshot`
- `POST /api/migrate-json`
- `GET/PUT /api/data`：兼容旧前端的聚合读写接口，内部读写拆表后的 PostgreSQL 数据。

存储要求：

- 页面逻辑不要直接散落调用 `localStorage`，统一通过数据读写方法访问。
- 远程模式下通过 API 读写 Supabase PostgreSQL。
- 远程接口失败时允许暂存到 `localStorage`，但需要提示用户。
- 图片字段使用对象结构，不直接只保存一个字符串，便于后续切换为服务器 URL 或对象存储。
- 导出 JSON 时需要包含图片信息；如果图片过大，后续版本可改为只导出图片地址。

## 5. 核心规则

### 5.1 菜谱所需食材汇总

一个菜谱的总消耗量等于所有步骤中同一食材消耗数量的合计。

例如某菜谱两个步骤都消耗鸡蛋：

- 步骤 1 消耗鸡蛋 1 个
- 步骤 2 消耗鸡蛋 2 个

则该菜谱总共需要鸡蛋 3 个。

### 5.2 可制作状态

菜谱状态分为：

- `可制作`：所有所需食材库存都满足。
- `缺少食材`：至少一个所需食材库存不足。

库存不足时需要展示缺少的食材名称和缺少数量，并用红色标识。

### 5.3 推荐排序

今日推荐和菜谱列表都按以下规则排序：

1. 所有所需食材都满足的菜谱排在最前面。
2. 使用快过期食材的菜谱优先展示。
3. 收藏菜谱优先展示。
4. 部分满足的菜谱排在后面。
5. 同一状态下，缺少食材种类更少的排在前面。
6. 最近已制作过的菜谱降低优先级。
7. 再按菜名排序。

### 5.4 制作完成扣库存

用户点击“制作完成”后：

1. 重新计算该菜谱需要的总食材数量。
2. 如果库存全部满足，则扣除对应库存。
3. 如果库存不足，则弹出提示，列出库存不足的食材，不扣任何库存。
4. 扣除成功后，从今日菜品中移除该菜。
5. 扣除成功后，写入做菜历史。

### 5.5 购物清单规则

用户可以从缺少食材提示中一键加入购物清单。

规则：

- 同一食材重复加入时合并数量。
- 购物清单项支持勾选、取消勾选、修改数量、删除。
- 购物完成后可一键将未删除的购物项加入冰箱库存。
- 加入库存后，对应购物项可自动清空或保持勾选状态，第一版默认自动清空已加入项。

### 5.6 保质期规则

- 食材可以设置保质期日期，也可以为空。
- 距离保质期 `meta.expireWarningDays` 天以内的食材标记为“临期”。
- `meta.expireWarningDays` 设置为 0 时，仅到期当天标记为“临期”。
- 已超过保质期的食材标记为“已过期”。
- 今日推荐优先推荐会使用临期食材的菜谱。
- 第一版只做提醒，不自动禁止使用过期食材。

### 5.7 数据导入导出规则

因为数据保存在浏览器本地，必须提供备份和恢复能力。

功能规则：

- 支持一键导出当前全部数据为 `.json` 文件。
- 导出内容包含分类、食材、菜谱、今日菜品、购物清单、做菜历史和版本信息。
- 支持从 `.json` 文件导入数据。
- 导入前需要校验 JSON 格式和必要字段。
- 导入成功后覆盖当前本地数据，并刷新页面状态。
- 导入失败时不能破坏现有本地数据。
- 提供“复制备份文本”和“从备份文本导入”作为备用方式。
- 提供“清空本地数据”和“重置示例数据”入口，危险操作必须二次确认。

### 5.8 页面排序规则

- 前台页面标签和后台页面标签都支持拖动排序。
- 拖动排序后立即保存到本地。
- 刷新页面后保持用户调整后的顺序。
- 排序只影响页面标签展示顺序，不影响业务数据。

## 6. 前台功能

### 6.1 今日推荐

根据当前库存展示推荐菜谱。

每个推荐卡片展示：

- 菜名
- 简短描述
- 可制作状态
- 总耗时
- 临期提示
- 精简缺少食材提示
- “加入今日菜品”按钮
- “加入购物清单”按钮，针对缺少食材显示

交互要求：

- 页面顶部需要展示醒目的操作提示，说明“双击菜品卡片查看完整详情”。
- 推荐卡片不展示“详情”按钮。
- 双击推荐卡片打开详情弹窗，用于查看完整食材、标签、难度和制作步骤。
- 可制作菜谱使用绿色状态标识。
- 缺少食材使用红色标识。
- 使用临期食材的菜谱显示提示。
- 已加入今日菜品的菜谱，按钮显示为“已加入”并禁用。
- 今日推荐页不展示“调整冰箱”和“新增菜谱”入口，保持推荐流简洁。

### 6.2 今日菜品

展示今天计划制作的菜品。

每个菜品展示：

- 菜名
- 餐次
- 所需总时长
- 当前可制作状态
- 开始制作按钮
- 移除按钮

功能要求：

- 今日菜品使用早餐、午餐、晚餐、加餐的切换模式展示，不使用四列并排布局。
- 切换餐次时只展示当前餐次的菜品，避免卡片过窄。
- 加入今日菜品时需要选择餐次，默认午餐。
- 点击“开始制作”进入制作步骤视图。
- 制作步骤视图展示当前步骤内容、步骤进度、步骤时长、计时器。
- 支持“上一步”、“下一步”、“暂停/继续”、“结束制作”。
- 如果当前步骤填写了耗时，则进入该步骤时显示计时器，并支持暂停/继续。
- 如果当前步骤未填写耗时，则不强制计时，只展示步骤内容和操作按钮。
- 到最后一步后显示“制作完成”。
- 点击“制作完成”执行库存扣除规则。

### 6.3 菜谱

展示所有菜谱。

功能要求：

- 支持按菜名和描述搜索。
- 支持按标签筛选。
- 支持按难度筛选。
- 支持按总耗时筛选，例如 15 分钟内、30 分钟内、60 分钟内。
- 支持只看收藏。
- 可按可制作状态自动排序。
- 支持查看菜谱详情。
- 支持收藏和取消收藏菜谱。
- 支持加入今日菜品。
- 提供“新增菜谱”入口，切换到后台菜谱管理。

菜谱详情展示：

- 菜名
- 描述
- 图片
- 标签
- 难度
- 是否收藏
- 所需食材汇总
- 总耗时
- 制作步骤
- 每一步时长和消耗食材

### 6.4 冰箱

展示所有食材库存。

功能要求：

- 使用分类切换模式展示，不同时展开所有分类。
- 点击分类标签后只展示当前分类的食材，避免页面过长和布局拥挤。
- 支持搜索食材。
- 页面顶部展示“临期食品预警”栏，集中展示临期和已过期食材。
- 临期食品预警栏中的食材支持双击打开修改弹窗。
- 页面只做查看，不在列表中直接编辑库存和保质期。
- 页面展示“双击食材卡片可修改库存和保质期”的提示。
- 双击食材卡片打开弹窗，支持修改库存和保质期。
- 冰箱列表中不显示“调整”按钮。
- 库存不能小于 0。
- 支持设置保质期。
- 临期和已过期食材需要有明显提示。
- 支持显示当前临期预警天数，并可从入口进入后台设置。

### 6.5 购物清单

展示需要购买的食材。

功能要求：

- 支持从推荐缺少食材一键生成购物项。
- 支持手动新增购物项。
- 支持勾选已购买。
- 支持修改购买数量。
- 支持删除购物项。
- 支持一键将已购买项加入冰箱库存。
- 加入库存后自动移除已购买项。

### 6.6 做菜历史

展示用户已经完成制作的菜品记录。

功能要求：

- 制作完成后自动写入历史。
- 展示菜名、制作时间。
- 支持按菜名搜索。
- 支持从历史中再次加入今日菜品。
- 今日推荐排序时，最近制作过的菜谱降低优先级。

## 7. 后台功能

### 7.1 食材分类管理

字段：

- 分类名称

功能：

- 新增分类
- 编辑分类
- 删除分类

删除规则：

- 如果分类下还有食材，不允许删除，并提示用户先处理食材。
- 分类列表页只展示数据和操作按钮；新增、编辑必须通过弹窗完成。

### 7.2 食材管理

字段：

- 食材名称
- 所属分类
- 库存数量
- 单位
- 保质期

功能：

- 新增食材
- 编辑食材
- 删除食材
- 调整库存

删除规则：

- 如果食材已被菜谱使用，删除前需要提示会影响相关菜谱。
- 确认删除后，从相关菜谱步骤消耗中同步移除该食材。
- 食材列表页只展示数据和操作按钮；新增、编辑必须通过弹窗完成。

### 7.3 菜谱管理

字段：

- 菜名
- 描述
- 菜品图片
- 标识颜色
- 标签
- 难度
- 是否收藏
- 制作步骤

步骤字段：

- 步骤说明：填写这个步骤要做什么。
- 步骤时长：非必填，单位为秒或分钟；填写后在做菜步骤页用于计时。
- 消耗食材：可选择多个食材。
- 消耗数量：每个食材对应填写消耗数量。

功能：

- 新增菜谱
- 编辑菜谱
- 删除菜谱
- 上传、预览、替换、删除菜品图片
- 收藏 / 取消收藏
- 新增、编辑、删除步骤
- 调整步骤顺序
- 为步骤添加多个消耗食材
- 菜谱列表页只展示数据和操作按钮；新增、编辑必须通过弹窗完成。

校验要求：

- 菜名必填。
- 难度必填。
- 至少需要一个制作步骤。
- 步骤说明必填。
- 步骤时长非必填；填写时必须大于 0。
- 消耗数量必须大于 0。
- 菜品图片非必填。
- 上传图片仅允许常见图片格式，例如 JPG、PNG、WebP。
- 当前版本限制单张图片大小，建议不超过 2MB；后续可将图片迁移到对象存储，只在数据库保存图片地址和元数据。

### 7.4 数据备份与恢复

展示当前数据状态，并提供备份恢复能力。远程模式下备份来源为 Supabase 数据库聚合快照，本地缓存仅作为兜底。

功能：

- 导出 JSON 文件。
- 导入 JSON 文件。
- 复制当前数据为备份文本。
- 从备份文本导入。
- 清空当前数据。
- 重置示例数据。
- 设置临期预警天数。

校验要求：

- 导入内容必须是合法 JSON。
- 导入内容必须包含 `categories`、`ingredients`、`recipes`、`todayDishes`、`shoppingList`、`cookHistory`。
- 导入失败时保留原数据。
- 导入成功后提示用户并重新渲染所有页面。
- 临期预警天数必须为大于等于 0 的整数。

## 8. 初始化数据

首次打开页面时，如果远程数据库和本地缓存都没有数据，需要写入一组示例数据，保证用户可以直接体验：

- 分类不少于 4 个。
- 食材不少于 10 个。
- 菜谱不少于 5 个。
- 标签不少于 6 个，直接存放在菜谱 `tags` 中即可，不需要单独做标签管理页面。
- 至少包含 2 个当前库存可制作的菜谱。
- 至少包含 2 个带临期食材的推荐场景。

提供“重置示例数据”按钮，方便用户恢复初始状态。该按钮放在“数据备份与恢复”页面，并需要二次确认。

## 9. UI 要求

- 整体风格：现代、简洁、清爽。
- 布局：顶部区域切换前台/后台，二级导航切换具体页面。
- 顶部摘要需要展示有行动价值的指标，不只展示基础总数。
- 顶部摘要至少包含：可做菜数量、临期食材数量、过期食材数量、今日计划数量、购物清单待购买数量、缺库存食材数量、最近做过的菜。
- 页面主体使用响应式布局，PC 端多列展示，移动端单列展示。
- 卡片圆角控制在 8px 左右。
- 按钮状态明确，包括默认、悬停、禁用、危险操作。
- 缺少食材用红色，可制作状态用绿色。
- 临期食材用醒目的暖色提示，已过期食材用危险色提示。
- 收藏菜谱需要有明显的星标或收藏按钮。
- 菜品图片需要在推荐卡片、菜谱列表和详情中展示；没有图片时显示简洁占位样式。
- 制作步骤页需要有进度条和清晰的计时器。
- 未设置耗时的步骤不显示倒计时压力，只展示步骤内容和切换按钮。
- 表单控件大小统一，适合移动端点击。
- 页面级提示需要足够明显，用于说明双击、拖拽、切换等非显性操作。
- 新增和修改操作统一使用弹窗表单；列表页只用于查看和删除，不做内联编辑。
- 冰箱库存和保质期修改通过双击食材卡片打开弹窗完成。
- 页面标签需要有可拖动排序的交互反馈。
- 不使用后端加载状态；所有交互在本地即时完成。

## 10. 验收标准

- 打开 `index.html` 后页面可正常显示。
- 刷新页面后，本地新增、编辑、删除的数据仍然保留。
- 顶部摘要能展示可做菜、临期、过期、今日计划、待购买、缺库存和最近做过的菜。
- 今日推荐能根据库存变化实时更新。
- 可制作菜谱排在不可制作菜谱之前。
- 缺少食材能显示名称和缺少数量。
- 缺少食材可一键加入购物清单。
- 购物清单已购买项可一键加入冰箱库存。
- 加入今日菜品后不会重复加入。
- 今日菜品可以按餐次切换查看，卡片不能被四列布局压窄。
- 制作完成后能正确扣除库存并移除今日菜品。
- 制作完成后能写入做菜历史。
- 库存不足时不会扣除任何库存，并能提示不足明细。
- 冰箱中临期、已过期食材能正确展示。
- 冰箱页有临期食品预警栏，集中展示临期和已过期食材。
- 冰箱按分类切换展示，不使用多个分类块同时展开的布局。
- 后台可设置临期预警天数，设置后冰箱和推荐的临期判断同步变化。
- 分类、食材、菜谱、购物项、冰箱调整的新增和修改都通过弹窗完成。
- 列表页不出现内联编辑表单，主要用于查看、编辑入口和删除。
- 冰箱页不显示调整按钮，双击食材卡片可以打开修改弹窗。
- 前台和后台页面标签可拖动排序，刷新后顺序保留。
- 菜谱支持标签、难度、耗时、收藏筛选。
- 菜谱管理可以上传、预览、替换、删除菜品图片。
- 菜谱步骤可以填写步骤说明、选择消耗食材、填写消耗数量，并可选择性填写耗时。
- 有耗时的步骤在制作时能正常计时；无耗时的步骤可以直接切换下一步。
- 数据访问通过统一存储方法完成，为后续接入数据库预留替换空间。
- 当前数据可导出为 JSON 文件。
- 合法备份 JSON 可成功导入并恢复页面状态。
- 非法导入不会破坏当前数据。
- 食材分类、食材、菜谱都能完成增删改查。
- 移动端宽度下页面不横向溢出，主要操作可正常点击。

## 11. 交付物

- `index.html`
- `styles.css`
- `app.js`
- 必要时可新增 `README.md`，说明运行方式和功能范围。
